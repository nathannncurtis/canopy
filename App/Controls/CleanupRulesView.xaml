<UserControl x:Class="SizeMonitor.Controls.CleanupRulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.Name="Cleanup rule preview">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="120" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,6">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Load JSON…" Padding="12,4" Margin="8,0,0,0"
                        Click="OnLoadJson" ToolTip="Load a versioned cleanup rule JSON file"
                        AutomationProperties.Name="Load cleanup rule JSON" />
                <Button Content="Save validated rules…" Padding="12,4" Margin="8,0,0,0" Click="OnSaveRules"
                        ToolTip="Validate and save this versioned declarative rule set" />
                <Button Content="Built-in cleanup scan" Padding="12,4" Margin="8,0,0,0"
                        Click="OnBuiltInPreview" ToolTip="Preview recognized temporary, log, dump, cache, update, and Recycle Bin storage"
                        AutomationProperties.Name="Preview built-in cleanup categories" />
                <Button Content="Personal storage scan" Padding="12,4" Margin="8,0,0,0"
                        Click="OnPersonalStoragePreview" ToolTip="Preview virtual disks, games, large attachment caches, downloads, backups, and orphaned app data"
                        AutomationProperties.Name="Preview personal storage categories" />
                <Button Content="Preview only" Padding="12,4" Margin="8,0,0,0"
                        Click="OnPreview" ToolTip="Evaluate rules without changing the filesystem"
                        AutomationProperties.Name="Preview cleanup rules" />
                <Button Content="Export audit…" Padding="12,4" Margin="8,0,0,0" Click="OnExportAudit"
                        ToolTip="Export a deterministic non-destructive cleanup preview report" />
            </StackPanel>
            <TextBlock VerticalAlignment="Center" FontWeight="SemiBold"
                       Text="Versioned cleanup rules (JSON)" />
        </DockPanel>

        <TextBox Grid.Row="1" x:Name="_ruleJson" AcceptsReturn="True" AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" TextWrapping="NoWrap" SpellCheck.IsEnabled="False"
                 ToolTip="Paste a version 1 cleanup rule document"
                 AutomationProperties.Name="Cleanup rule JSON editor" />

        <TextBlock Grid.Row="2" Margin="0,8,0,8" TextWrapping="Wrap"
                   Text="Preview is non-destructive: no file or directory is deleted, moved, or modified. Reclaimable bytes are estimates from scan allocation data; inspect every match before taking action outside Canopy."
                   AutomationProperties.Name="Cleanup preview safety notice" />

        <Grid Grid.Row="3" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" x:Name="_riskFilter" Width="140" SelectedIndex="0"
                      SelectionChanged="OnFilterChanged" Margin="0,0,8,0"
                      ToolTip="Filter preview matches by risk"
                      AutomationProperties.Name="Cleanup risk filter">
                <ComboBoxItem Content="All risk levels" />
                <ComboBoxItem Content="Low risk" />
                <ComboBoxItem Content="Medium risk" />
                <ComboBoxItem Content="High risk" />
            </ComboBox>
            <TextBox Grid.Column="1" x:Name="_ruleFilter" Width="150" Margin="0,0,8,0"
                     TextChanged="OnFilterChanged" ToolTip="Filter by rule ID"
                     AutomationProperties.Name="Rule ID filter" />
            <TextBox Grid.Column="2" x:Name="_reasonFilter" Width="190" Margin="0,0,8,0"
                     TextChanged="OnFilterChanged" ToolTip="Filter by cleanup reason"
                     AutomationProperties.Name="Cleanup reason filter" />
            <TextBlock Grid.Column="3" x:Name="_status" VerticalAlignment="Center"
                       Text="Load or paste rules, then run a scan to preview."
                       AutomationProperties.LiveSetting="Polite"
                       AutomationProperties.Name="Cleanup preview status" />
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="_previousFinding" Content="Previous" Click="OnPreviousFinding"
                    IsEnabled="False" Padding="12,4" Margin="0,0,8,0" />
            <Button x:Name="_nextLargestFinding" Grid.Column="1" Content="Reveal next largest"
                    Click="OnNextLargestFinding" IsEnabled="False" Padding="12,4" Margin="0,0,12,0" />
            <TextBlock x:Name="_explanation" Grid.Column="2" TextWrapping="Wrap"
                       Text="Select a recommendation to see why it matched and what to review."
                       AutomationProperties.LiveSetting="Polite"
                       AutomationProperties.Name="Cleanup recommendation explanation" />
        </Grid>

        <Border Grid.Row="5" Margin="0,0,0,8" Padding="10" BorderThickness="1" BorderBrush="#44888888">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" /><ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="_queueNote" Margin="0,0,8,0"
                         ToolTip="Optional note for the selected cleanup recommendation"
                         AutomationProperties.Name="Cleanup queue note" />
                <TextBox Grid.Column="1" x:Name="_queueTags" Margin="0,0,8,0"
                         ToolTip="Comma-separated tags"
                         AutomationProperties.Name="Cleanup queue tags" />
                <Button Grid.Column="2" Content="Mark for later" Padding="12,4" Margin="0,0,8,0"
                        Click="OnQueueSelected" AutomationProperties.Name="Add selected recommendation to cleanup queue" />
                <Button Grid.Column="3" Content="Remove mark" Padding="12,4" Margin="0,0,8,0"
                        Click="OnRemoveQueued" AutomationProperties.Name="Remove selected recommendation from cleanup queue" />
                <TextBox Grid.Column="4" x:Name="_confirmationThreshold" Width="105" Margin="0,0,8,0"
                         ToolTip="Typed-confirmation threshold in bytes" AutomationProperties.Name="Cleanup confirmation threshold bytes" />
                <Button Grid.Column="5" Content="Save threshold" Padding="12,4" Click="OnSaveThreshold" />
                <StackPanel Grid.Row="1" Grid.ColumnSpan="6" Orientation="Horizontal" Margin="0,8,0,0">
                    <ComboBox x:Name="_cleanupMode" Width="190" SelectedIndex="0" Margin="0,0,8,0"
                              AutomationProperties.Name="Cleanup operation mode">
                        <ComboBoxItem Content="Windows Recycle Bin" />
                        <ComboBoxItem Content="Canopy app-owned recovery" />
                        <ComboBoxItem Content="Permanent deletion" />
                    </ComboBox>
                    <CheckBox x:Name="_recursiveCleanup" Content="Recursive directories" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <Button Content="Preview queued operation" Padding="12,4" Click="OnPreviewQueuedOperation" Margin="0,0,8,0" />
                    <TextBox x:Name="_operationConfirmation" Width="260" Margin="0,0,8,0"
                             ToolTip="Type the exact confirmation phrase shown by preview" />
                    <Button Content="Execute confirmed operation" Padding="12,4" Click="OnExecuteQueuedOperation" />
                    <Button Content="Undo Canopy recovery" Padding="12,4" Margin="8,0,0,0" Click="OnUndoRecovery" />
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="6" x:Name="_items" AutoGenerateColumns="False"
                  IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  SelectionChanged="OnItemSelectionChanged"
                  MouseDoubleClick="OnItemDoubleClick"
                  ToolTip="Double-click a preview match to activate its scan node"
                  AutomationProperties.Name="Cleanup preview matches">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Index" Binding="{Binding NodeIndex}" Width="70" />
                <DataGridTextColumn Header="Risk" Binding="{Binding Risk}" Width="90" />
                <DataGridTextColumn Header="Relative path" Binding="{Binding Path}" Width="*" />
                <DataGridTextColumn Header="Bytes" Binding="{Binding ReclaimableBytes, StringFormat=N0}" Width="115" />
                <DataGridTextColumn Header="Rules" Binding="{Binding RuleText}" Width="170" />
                <DataGridTextColumn Header="Reasons" Binding="{Binding ReasonText}" Width="230" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
