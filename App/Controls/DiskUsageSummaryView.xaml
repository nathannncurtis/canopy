<UserControl x:Class="SizeMonitor.Controls.DiskUsageSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.Name="Disk usage summary">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock FontSize="22" FontWeight="SemiBold" Text="What is using this storage?" />
    <TextBlock x:Name="_status" Grid.Row="1" Margin="0,8,0,12" TextWrapping="Wrap"
               AutomationProperties.LiveSetting="Polite" />
    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <GroupBox Header="Largest folders" Margin="0,0,8,0">
        <ListView x:Name="_folders" MouseDoubleClick="OnFolderActivated"
                  KeyDown="OnFolderKeyDown" AutomationProperties.Name="Largest folders">
          <ListView.View>
            <GridView>
              <GridViewColumn Header="Folder" DisplayMemberBinding="{Binding Name}" Width="240" />
              <GridViewColumn Header="Size" DisplayMemberBinding="{Binding DisplayBytes}" Width="110" />
            </GridView>
          </ListView.View>
        </ListView>
      </GroupBox>
      <GroupBox Grid.Column="1" Header="File categories" Margin="8,0,0,0">
        <ListView x:Name="_categories" AutomationProperties.Name="Largest file categories">
          <ListView.View>
            <GridView>
              <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Name}" Width="170" />
              <GridViewColumn Header="Size" DisplayMemberBinding="{Binding DisplayBytes}" Width="110" />
              <GridViewColumn Header="Share" DisplayMemberBinding="{Binding Share}" Width="80" />
            </GridView>
          </ListView.View>
        </ListView>
      </GroupBox>
    </Grid>
  </Grid>
</UserControl>
